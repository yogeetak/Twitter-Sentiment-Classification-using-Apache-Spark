Submitted By:
Janani Neelakantan, Kuttabadkar Yogeeta Monica

Please install the following libraries before running;
1. nltk
2.nltk.download- prefabably all corpuses
3. Some dependencies exist between cells where functions are called. In such cases, ensure that the method cell is executed first, followed by the invocation.
4. The data train.csv, test.csv is uploaded and availale within the spark context on jupter itsef. No external link is needed or given
5. The files need to be opened and saved in the system, before uploading. This ensures that the encoding is corrected to csv format. The encoding is not handled within the program.

All references are mentioned in the index.html file as links to the website.  